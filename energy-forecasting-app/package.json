{
  "name": "energy-forecasting-app",
  "version": "1.0.0",
  "description": "Full-stack energy forecasting application with LSTM/GRU models",
  "scripts": {
    "start": "./start.sh",
    "backend": "cd backend && source venv/bin/activate && python main.py",
    "frontend": "cd frontend && npm run dev",
    "install:backend": "cd backend && pip install -r requirements.txt",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend"
  },
  "keywords": ["energy", "forecasting", "lstm", "gru", "react", "fastapi"],
  "author": "Energy Forecasting Team",
  "license": "MIT"
}
